services:
  web:
    build: .
    container_name: terrapulse
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ALLOWED_HOSTS=127.0.0.1,localhost,0.0.0.0
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
